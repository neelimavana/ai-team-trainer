# Python
__pycache__/
*.py[cod]
*.egg-info/

# Virtual environment (uv)
.venv/

# Tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/

# IDE
.vscode/
.idea/

# Runtime artifacts
# NOTE: scores.csv / forgetting_report.png are required deliverables and are
# intentionally tracked. Trained policy zip files are storage-heavy transient
# artifacts and stay untracked — they are reproducible from config + seed.
outputs/*.zip

# Local scratch logs
/logs/

# Generated by the Phase 7 parity check (Python scripts/logs/phase7_parity.py).
# config.parity.yaml itself is committed so the DoD check is reproducible.
/config.parity2.yaml